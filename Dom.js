//---------->DOM:Document Object Model <----------//

//The DOM is a programming interface for web documents. It enables dynamic interaction and manipulation of web documents within a browser environment.


//---------->Window:<----------//

// 1.)The window is at a root/top level at the JavaScript object hierarchy. It  is a global/root object in JavaScript and it is the root object of the Document object model(DOM).
// 2.)The first thing that gets loaded into the browser is the window and the properties related to that window are stored in the window object.
// Syntax:window.propertyname;

//---------->Document:<----------//

// 1.)A document is an object inside the window object and we use a document object for manipulation inside the document.
// 2.)After the window gets loaded then there’s a document (HTML, PHP, or another document) loaded inside that window, and the properties related to that document is stored in the document object.
// Syntax:document.propertyname OR window.document.propertyname



